import 'package:flutter/material.dart';

class AppStyles {
  static const TextStyle titleText = TextStyle(
    fontSize: 17,
    fontWeight: FontWeight.bold,
    color: Colors.black87,
  );

  static const EdgeInsets sectionPadding = EdgeInsets.all(5);

  static BorderRadius defaultRadius = BorderRadius.circular(15);
}
